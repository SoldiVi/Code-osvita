+++
draft = false
title = 'Лабораторна робота №6'
+++

## Тема: Одновимірні масиви в Python

---

## Мета роботи

Навчитися створювати найпростіші програми на мові Python, використовуючи одновимірні масиви інформації, списки, алгоритми обробки одновимірних масивів. Сортування, знаходження екстремумів.

---

## Теоретичні відомості

**Масив** – впорядкований скінчений набір даних одного типу, які зберігаються в послідовно розташованих комірках оперативної пам'яті і мають спільну назву, яку надає користувач.

Масиви бувають одновимірні та двовимірні. Одновимірний масив має вигляд таблиці, що складається з одного рядка і багатьох стовпців.

Оскільки масиви подаються у вигляді списків, то нумерація їх елементів починається з нуля і на них діють всі методи, які використовуються для списків. Єдине, що список може містити різні типи даних, а масиви тільки один (числовий, текстовий, булевий).

Для того, щоб розв'язувати задачі з масивами, потрібно виконати наступні кроки:

1. Створити масив, задати його тип та наповнити його елементами
2. Виконати дії з масивом за умовою задачі
3. Вивести масив (цей чи інший) чи результат виконання задачі на екран

---

## Методи списків

| Метод | Опис |
|-------|------|
| `append(x)` | додає елемент зі значенням `x` в кінець послідовності |
| `clear()` | очищує список |
| `copy()` | створює копію списку |
| `count(x)` | повертає кількість елементів зі значенням `x` |
| `extend(s)` | розширює список, додаючи до кінця поточного списку список `s` |
| `index(x[,j0[,j1]])` | повертає найменший індекс елемента зі значенням `x` [для номерів елементів від `j0` [до `j1`]]. Породжує виключення `ValueError`, якщо елемента з таким значенням не знайдено |
| `insert(j, x)` | вставляє на місце з номером `j` елемент зі значенням `x` |
| `pop(j)` | видаляє елемент з номером `j` з масиву |
| `del(a[i])` | вилучає `i` елемент масиву |
| `remove(x)` | вилучає елемент з найменшим індексом серед тих, що мають значення `x`. Породжує виключення `ValueError`, якщо елемента з таким значенням не знайдено |
| `reverse()` | змінює порядок елементів на зворотний |
| `sort()` | упорядковує елементи; якщо аргумент `reverse=True`, то сортування відбувається в порядку спадання |
| `min(a)` | повертає найменший елемент масиву |
| `max(a)` | повертає найбільший елемент масиву |
| `len()` | повертає довжину масиву (кількість елементів у масиві) |

---

## Способи створення й заповнення масивів

### 1. Введення вручну

```python
a = [2, 3, 5, 7, 11, 13]
Rainbow = ['червоний', 'оранжевий', 'жовтий', 'зелений', 'блакитний', 'синій', 'фіолетовий']
```

### 2. Створити порожній список і додавати елементи методом `append`

```python
a = []
n = int(input("Введіть кількість елементів масиву"))
for i in range(n):
    x = int(input())
    a.append(x)
```

> Останні 2 рядки можна замінити одним: `a.append(int(input()))`

### 3. Заповнити випадковими числами за допомогою `randrange`

```python
from random import randrange
n = 10
a = [randrange(1, 10) for i in range(n)]
```

> Масив можна заповнювати як додатними, так і від'ємними елементами: `randrange(-10, 10)`

---

## Способи виведення масивів

### 1. Вказівка `print`

```python
print(a)
```

### 2. За допомогою циклу `for`

**Спосіб 1** — змінюючи індекс `i`, виводити елемент списку з індексом `i`:

```python
for i in range(len(a)):
    print(a[i], end=' ')
```

**Спосіб 2** — змінюючи не індекс елемента списку, а його значення:

```python
for elem in a:
    print(elem, end=' ')
```

---

## Приклади

### Приклад 1
Створити масив з 10 елементів. Вивести елементи масиву, що стоять на парних місцях.

```python
a = [1, 2, 35, -6, 5, 6, 6, 4, 3, 2]
b = []
print(a)
for i in range(len(a)):
    if i % 2 == 0:
        b.append(a[i])
print(b)
```

### Приклад 2
Створити масив з 10 елементів. Вивести непарні елементи масиву.

```python
a = [1, 2, 35, -6, 5, 6, 6, 4, 3, 2]
b = []
print(a)
for i in range(len(a)):
    if a[i] % 2 == 1:
        b.append(a[i])
print(b)
```

---

## Хід роботи

### Завдання 1
Дано одновимірний масив. Скласти програму:

- **а)** розрахунку квадратного кореня із будь-якого елементу масиву;
- **б)** розрахунку середнього арифметичного двох будь-яких елементів масиву;
- **в)** чи є s-й елемент масиву додатнім числом;
- **г)** чи є k-й елемент масиву парним числом;
- **д)** який елемент масиву більший: k-й чи s-й.

### Завдання 2
Дано масив. Усі його елементи:

- **а)** збільшити в 2 рази;
- **б)** зменшити на число А;
- **в)** поділити на перший елемент;
- **г)** впорядкувати масив за зростанням значень.

### Завдання 3
Дано масив цілих чисел. Вияснити:

- **а)** чи вірно, що сума елементів масиву є парним числом;
- **б)** чи вірно, що сума квадратів елементів масиву є п'ятизначним числом.

### Завдання 4
В масиві зберігається інформація про чисельність учнів в кожному із 42 груп коледжу. Вияснити, чи вірно, що загальна кількість учнів в коледжі є чотиризначним числом. Знайти найбагаточисельну групу учнів і вивести кількість учнів в групі. Знайти найменшу групу в коледжі і вивести кількість учнів у ній. Порівняти, чи середнє значення цих двох груп перевищує загальне середнє значення по всьому коледжу, чи ні.

---

## Контрольні запитання

1. Що таке одновимірний масив, для чого його використовують?
2. Яким чином можна вводити елементи в масив?
3. Яким чином можна виводити елементи масиву на екран?
4. Яким чином заповнити масив випадковими числами?
5. Яким чином передбачити заповнення масиву користувачем з клавіатури?
6. Які команди можна використовувати для роботи з масивами?

## [Перевірити знання](../../tests/lab6/test.html)

