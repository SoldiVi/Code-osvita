+++
draft = false
title = 'Лабораторна робота №7'
+++

## Тема: Двовимірні масиви в Python

## Мета роботи

Навчитися створювати найпростіші програми на мові Python, використовуючи двовимірні масиви інформації, списки списків, алгоритми обробки двовимірних масивів. Сортування, знаходження екстремумів.

---

## Теоретичні відомості

**Масив** — це структура даних, яка використовується для зберігання елементів. Масив може зберігати лише подібні типи елементів. Двовимірність визначається як масив усередині масиву. Індекс масиву починається з 0 і закінчується розміром масиву мінус 1. Ми можемо створити `n` кількість масивів у масиві.

Для того, щоб розв'язувати задачі з масивами, потрібно виконати наступні кроки:

1. Створити масив, задати його тип та наповнити його елементами
2. Виконати дії з масивом за умовою задачі
3. Вивести масив (цей чи інший) чи результат виконання задачі на екран

---

## Способи створення й заповнення масивів

### 1. Створення порожнього списку

Створення порожнього списку `a` з 3 рядків:

```python
a = [[], [], []]
```

Нехай потрібно створити список для збереження даних прямокутної таблиці, у якій `n` рядків і `m` стовпців, і заповнити його нулями:

```python
n, m = 3, 4
a = []                   # Створюємо порожній список
for i in range(n):       # Додаємо n елементів
    a.append([0] * m)    # Кожен елемент — список з m нулів
```

### 2. Введення за допомогою генератора

Двовимірний масив можна створити з використанням генератора. Список `[0]*m` заново генерується для заповнення чергового елемента списку `a`:

```python
a = [list(input().split()) for i in range(n)]
```

### 3. Уведення значень двовимірного масиву з клавіатури

Щоб заповнити двовимірний список цілими значеннями елементів двовимірного масиву з `n` рядків і `m` стовпців, уводячи значення елементів з клавіатури, слід `n` разів виконати дії:

1. увести рядок `s`, що містить `m` чисел, відокремлених пробілами, і розбити рядок `s` функцією `split()` на підрядки;
2. функцією `map()` кожний підрядок рядка `s` перетворити на числовий тип;
3. функцією `list()` отриману послідовність перетворити у список `row`;
4. список `row` додати до списку `a`.

```python
n = int(input())    # Кількість рядків масиву
a = []              # Створюється порожній список
for i in range(n):
    s = input().split()
    row = list(map(int, s))
    a.append(row)
```

### 4. Заповнення двовимірного масиву випадковими числами

```python
from random import randint
n, m = 3, 4
a = [[] for i in range(n)]
for i in range(n):
    for j in range(m):
        a[i].append(randint(1, 10))    # Додавання чергового елемента до і-го рядка
print(a)
```

Результат виведення списку `a` в консоль може бути таким:

```
[[7, 4, 4, 2], [10, 9, 1, 10], [6, 7, 2, 2]]
```

### 5. Надання значень елементам двовимірного масиву за формулою

Задання значень елементів двовимірного масиву за певною формулою має місце у випадках, коли значення елемента залежить від його індексів.

---

## Доступ до значень масиву

Ми можемо отримати доступ до значень за допомогою позиції індексу:

```python
array[row_index]              # Доступ до рядка
array[row_index][col_index]   # Доступ до конкретного елемента
```

де:
- `row_index` — позиція індексу рядка, яка починається з `0`
- `col_index` — позиція індексу стовпця, яка починається з `0` у рядку

---

## Приклади

### Приклад 1
Отримати доступ до значень за допомогою позицій індексу.

```python
# Створення 2D масиву з 4 рядків і 5 стовпців
array = [[23, 45, 43, 23, 45], [45, 67, 54, 32, 45], [89, 90, 87, 65, 44], [23, 45, 67, 32, 10]]

print(array)          # увесь масив
print(array[0])       # перший рядок
print(array[2])       # третій рядок
print(array[0][2])    # перший рядок, третій елемент
print(array[2][3])    # третій рядок, четвертий елемент
```

Результат:

```
[[23, 45, 43, 23, 45], [45, 67, 54, 32, 45], [89, 90, 87, 65, 44], [23, 45, 67, 32, 10]]
[23, 45, 43, 23, 45]
[89, 90, 87, 65, 44]
43
65
```

### Приклад 2
У масиві 3×4 замінити елементи, що кратні 3, числом 100. Вивести початковий та змінений масиви.

```python
b = [[77, 32, 23, 3], [44, 21, 23, 9], [80, 15, 22, 4]]
print("Дано масив")
for i in range(3):
    print(' '.join(map(str, b[i])))

print("Змінено елементи масиву, кратні 3 числом 100")
for i in range(3):
    for j in range(4):
        if b[i][j] % 3 == 0:
            b[i][j] = 100
for i in range(3):
    print(' '.join(map(str, b[i])))
```

### Приклад 3
У масиві 3×3 поміняти місцями рядки: перший та останній.

```python
a = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
print("Дано масив:")
for i in a:
    for j in i:
        print(j, end=" ")
    print()

for j in range(3):
    p = a[0][j]
    a[0][j] = a[2][j]
    a[2][j] = p

print("Змінений масив:")
for i in a:
    for j in i:
        print(j, end=" ")
    print()
```

Результат:

```
Дано масив:
1 2 3
4 5 6
7 8 9
Змінений масив:
7 8 9
4 5 6
1 2 3
```

---

## Хід роботи

### Завдання 1
У даній дійсній матриці розмірністю 6×9 знайти суму елементів рядка, що містить найбільший елемент. Вважається, що такий елемент єдиний.

### Завдання 2
Дано матрицю M×N. У цій таблиці поміняти місцями елементи:

- **а)** першого і останнього рядків;
- **б)** першого і останнього стовпчика;
- **в)** k-того та l-того стовпчика.

### Завдання 3
Дано квадратну матрицю розмірності `n`. Надрукувати:

- **а)** індекси елементів, розташованих на бічній діагоналі;
- **б)** суму елементів бічної діагоналі;
- **в)** елементи бічної діагоналі в порядку зростання.

### Завдання 4
Знайти різницю між найбільшим і найменшим елементом головної діагоналі квадратної матриці розмірністю `n`.

### Завдання 5
Таблицю футбольного чемпіонату задано квадратною матрицею порядку `n`, в якій усі елементи, що розміщені по головній діагоналі, дорівнюють 0, а кожний елемент, що не належить головній діагоналі, дорівнює 2, 1 або 0 очок, що набрані в грі: `2` — виграш, `1` — нічия, `0` — програш. Визначити:

- **а)** кількість команд, які мають більше перемог, ніж поразок;
- **б)** номери команд, що не мають поразок;
- **в)** чи є хоча б одна команда, що виграла більше половини ігор.

---

## Контрольні запитання

1. Що таке двовимірний масив, для чого його використовують?
2. Яким чином можна вводити елементи в масив?
3. Яким чином можна виводити елементи масиву на екран?
4. Яким чином заповнити масив випадковими числами?
5. Яким чином передбачити заповнення масиву користувачем з клавіатури?
6. Які команди можна використовувати для роботи з масивами?
7. Які формули для знаходження індексів головної і побічної діагоналі?

## [Перевірити знання](../../tests/lab6/test.html)
